import React from "react";

const test = () => {
    let num: number = 67;

    return (
        <div
            className={`all styles ${defaultColor[index]} ${
                blackIndex === index ? "border border black" : ""
            }`}></div>
    );
};

export default test;
